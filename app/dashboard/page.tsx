import { DashboardOps } from "@/registry/blacksite/blocks/dashboard-ops";

export const metadata = {
  title: "Birthday Ops — Blacksite UI",
};

export default function DashboardPage() {
  return <DashboardOps />;
}
